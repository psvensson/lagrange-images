# Ownership registry

This is the authoritative map of architectural ownership for Lagrange Images.

The rule is strict:

> Every major subsystem has one owner. Every interaction between subsystems has one owner.

An owner is one architectural code locus — a service, module, registry, adapter, composition root, or lower/external layer. It is not a claim about a human maintainer.

The owner owns the concern's semantic decisions, state-transition/public contract and primary proof surface. Other components may call, observe or adapt it; they may not independently decide the same rule.

A planned owner reserves responsibility but does not claim implementation exists yet.

## Subsystem owners

| Responsibility | Single owner | Location/status | Notes |
| --- | --- | --- | --- |
| Runtime composition and package-wide assembly | `createRuntime()` composition root | `src/runtime.js` | Wires owners together; must not absorb their semantic policy. |
| Durable image graph operations and history publication | `GraphImageService` | `src/image/graph-image-service.js` | Owns image-record operations above the backend seam. Also owns the atomic heterogeneous insert-only creation primitive (`createRecords`): one record-kind dispatch (`prepareCandidateRecord`/`validateCandidateRecord`/`putEventForRecord`) shared by the single-record puts and both batches, and one atomic commit owner (`commitCandidateRecords`) for "N records + N per-kind history events -> one backend transaction". The backend stays kind-unaware. |
| Canonical Value semantics | Value model | `src/value/` | Smallest common value layer; language semantics do not leak into it. |
| Generic Shape/Object representation | Object model | `src/object/` | Owns language-neutral object layout/validation. |
| Portable graph bundle semantics (closure, bundle-local identity, external-ref rule, canonical form, content identity, bundle validation, external-resolution contract, bundle-local -> target-local identity translation) | Graph bundle model | `src/graph/bundle.js` (ADR 0074, EXPORT + IMPORT implemented) | Owns the language-neutral transitive-graph materializer in BOTH directions. Consumes `referencesOfRecord` (edge enumeration AND canonical edge order stay owned there) and record payloads; never duplicates graph storage or re-implements per-kind traversal. Traversal order is driven by `referencesOfRecord`; the generic projection rewrites exactly the refs the edge owner enumerates (executable guard). `contentIdentity` = SHA-256 over the canonical form via the crypto provider (`contentIdentityForBundle`, one owner for both directions); metadata/`updatedAt`/`_version`/source provenance/frontier excluded from identity. `assertGraphBundleV1` is the single owner of bundle validity (structure + closure completeness); the exporter self-checks through it post-assembly. Import mints all target ids up front (active provider `uuid()`, never derived from localId/source id/contentIdentity/root key), rewrites refs generically, and publishes ONCE through `ImageService.createRecords`. An authorized export/import lane is a separate future owner. |
| CodeArtifact representation and graph semantics | Code artifact model | `src/code/` | Owns durable code/artifact identity, dependencies and provenance fields. |
| Storage transaction contract | Backend contract | `src/backend/` | Mock and durable adapters implement one transaction/storage seam. |
| Durable Lagrange storage mapping | Lagrange backend adapter | `src/backend/create-backend.js` plus Lagrange backend implementation | Owns translation from the backend contract to public Lagrange application sessions. |
| Invocation construction and language dispatch entry | `InvocationService` | `src/dispatch/invocation-service.js` | Turns a language-level application/send into an activation through the selected dispatcher. |
| Activation lifetime, recursive execution and execution context | `ActivationExecutor` | `src/execution/executor.js` | Owns activation lifetime, recursive sends, lexical execution context and executor invocation. |
| Executable representation selection | Code executor registry | `src/execution/` | Selects exactly one executor by CodeArtifact representation. |
| Transient authority algebra and contexts | `AuthorityService` | `src/authority/authority-service.js` | Owns issue/attenuate/revoke/require semantics; authority never becomes program data. |
| Image-native compilation orchestration | `CompilationService` | `src/compilation/` | Owns compiler/group-compiler invocation and derived executable publication. |
| External toolchain orchestration and derivation reuse | `ToolchainService` | `src/toolchain/` | Owns explicit input resolution, provider invocation, result provenance and reuse policy. |
| Foreign runtime lifecycle | `ForeignRuntimeService` | `src/foreign-runtime/` | Owns transient start/call/stop lifecycle and provider-private runtime handles. |
| Durable foreign-runtime definition resolution | `ForeignRuntimeDefinitionService` | `src/foreign-runtime/` | Owns durable definition -> runtime-local provider/startable-definition resolution. |
| Callable/interface and implementation-binding contracts | Callable layer | `src/callable/` | Owns language-neutral callable/interface/binding representations and authorized image bindings. |
| Internal and foreign WASM execution machinery | WASM layer | `src/wasm/` | Owns Value-handle ABIs, module/component adapters, caches and WASM-specific execution contracts. |
| Symmetric Smalltalk language semantics | Symmetric Smalltalk personality | `src/language/` | Owns syntax, lookup, class/kernel/library semantics and language-specific compilation policy. |
| Image-level Project working-state semantics | Project image library/service | current: `src/project/working-state.js` | Durable Project objects (`lagrange-project/project/v1`), member objects (`lagrange-project/member/v1`) and the create/add/read translation over ordinary image objects/refs; no special backend record type. Member key is identity (target is a mutable slot); membership conveys no authority. Reads hand the assembled record to `normalizeProjectDescriptor` — the pure model stays the sole owner of descriptor semantics. Owns `authorizedReadProjectDescriptor`, the single authorized semantic Project read: ONE authorized `object/read` on the Project object authorizes reading the Project's own backing member records (they are the Project's storage representation, NOT independent authority units — this does NOT follow member targets, whose content still needs its own `object/read`), authorized before existence disclosure (no-existence-oracle), returning the canonical descriptor with no backing/Shape/slot ids escaping. Working-frontier/diff/merge and human Project interaction remain above this slice. |
| Project release/deployment semantic contracts | Project model | current: `src/project/model.js` (ADR 0073) | Owns portable `projectId` and Project-local member keys, explicit DeploymentProfile selection, canonical release identity, separate source provenance/frontier map, target-specific ProjectInstallation mapping and effect-free upgrade planning. Does not persist Project objects, materialize releases or perform deployment effects. |
| Agent work/dependency graph | Beads | `.beads`/Dolt after `bd init` | Authoritative operational task tracker; no parallel Markdown TODO tracker. |
| Host-portable binary primitives | Portable bytes | `src/support/portable-bytes.js` | Owns the single internal home for language-neutral binary machinery — `Uint8Array`/`DataView`/`TextEncoder`/`TextDecoder` and internal base64/base64url/hex. Binary layout semantics stay owned here; Node `Buffer` is never a host capability, never accepted or returned. On-wire bytes are frozen by compatibility (byte-for-byte identical to the prior Buffer implementation, proven differentially). |
| Host crypto/random primitive seam | Crypto provider | `src/support/crypto-provider.js` (portable contract/validation, no Node imports) + `src/support/node-crypto-provider.js` (Node reference impl, imported ONLY by the Node composition root) + `src/support/default-crypto.js` (active-default registry, no Node imports) | Owns the narrow SYNCHRONOUS host primitive contract (`secureRandomBytes`/`sha256`/`aes256gcmEncrypt`/`aes256gcmDecrypt`/`uuid`). Computes primitives ONLY — it is NOT modeled on the broad `node:crypto` API. All policy (observation cursor format, AES-GCM IV/tag layout, what is authenticated, key derivation, parsing, integrity failure) stays in the consuming semantic module (e.g. `image-observation-binding`), never here. The contract is synchronous because `typeFingerprint`/version-token/observation-cursor run deep inside synchronous executor paths; hosts whose only crypto is async are a separate finding, not a shim target. Contract/validation is separated from the Node reference implementation so the portable runtime's static closure never reaches `node:crypto`; the Node root installs the reference provider at load, a portable host installs its own via `setDefaultCryptoProvider`. |
| Portable runtime composition | Portable runtime root | `src/portable-runtime.js` (core + portable root) | Owns the single portable composition root for the language-neutral Image/authority/dispatch/observation acceptance subset. Its complete STATIC transitive ESM closure has no `node:crypto`/`buffer`/`child_process`/`readline`/`fs`/`path`/`os`/process-dependent toolchain runner — proven by a structural closure-walking test. Wiring/export selection ONLY, not a second implementation: it imports the narrow registry/service owners directly (never the broad `foreign-runtime`/`toolchain`/`wasm` barrels or the Node crypto provider) and composes the portable-relevant lanes (image create/read/mutate/observe, neutral expression, the Smalltalk kernel). `createRuntimeCore` is the shared semantic core; the Node root (`src/runtime.js`) adds foreign-runtime/toolchain/WASM-component services/executors on top. A portable host installs its sync crypto provider before composing, and does so through this root: it RE-EXPORTS `setDefaultCryptoProvider` — the identical function `src/support/default-crypto.js` owns, not a wrapper, second registry or host object. The same bounded public root re-exports the exact existing language/callable/value/authority/object functions required to construct Object Environment's `ImageClientAdapter`; those modules retain all semantic ownership and strict-identity tests forbid wrappers. A portable Object Environment therefore needs no private `src/...` module path, broad barrel or second facade. This root owns only public exposure/configuration; provider validation stays in `crypto-provider.js` and active-provider installation stays in `default-crypto.js`. |
| Portable runtime source artifact (`lagrange-images-portable-runtime/v1`) | Portable runtime artifact (`src/portable-artifact/`, produced via `scripts/build-portable-runtime-artifact.mjs`) | current (bead lagrange-images-z42) | ONE owner for deterministic `lagrange-images-portable-runtime/v1` production, static portable-module closure derivation, and artifact validation. Several files, ONE owner: closure derivation (`module-closure.js`), format/validation/identity (`portable-runtime-artifact.js`), source reader (`node-source-reader.js`) and the producer entrypoint are implementation loci beneath this single owner — they are NOT separate subsystem owners, and closure traversal is NOT owned in two places. THE KEY INVARIANT: the static portable closure has ONE definition, consumed by BOTH the existing portable-runtime structural proof (`test/portable-runtime.test.js` routes through it) and artifact generation/validation; there is no test walker versus shipping walker, and a structural proof fails if a second module declares the format or defines the closure. PACKAGING/PROVENANCE ONLY — this code packages the runtime and owns NO runtime semantics: `src/portable-runtime.js` remains THE portable runtime composition root, `support/default-crypto.js` owns active-provider installation, `support/crypto-provider.js` owns provider validation. `entry` is the REAL `src/portable-runtime.js` module (never a generated wrapper), so the artifact exposes exactly what the portable root exposes, including the `setDefaultCryptoProvider` seam — a consumer never needs a `src/support/*` path. Modules ship as exact UTF-8 ESM sources with relative specifiers preserved: no bundling, minification, transpilation or rewriting, because the module graph and its cycles are semantically load-bearing. Canonical material is `{format, entry, modules}` in logical-path order with no absolute path, mtime, inode or enumeration order; `contentIdentity` is `sha256:<hex>` over those bytes, and a source revision is recorded as `provenance` OUTSIDE that material so identical source material keeps one identity across commits. Static closure follows relative specifiers only — a dynamic `import(variable)` (the gated `lagrange-server` host integration) is deliberately not chased — and any `node:*` or bare static import, unresolved import, source-root escape, or mismatch between declared closure and shipped modules is refused by the Images-owned validator, never left for a consumer loader to discover. Object Environment owns ONLY loading/evaluating the artifact's `entry`; it does not compose the artifact, reconstruct the closure, or invent the format. |
| Short durable agent discoveries | Beads memory | `bd remember` | Promote architectural/behavioral truth into tests/docs/ADRs as well. |
| Architecture decision history | ADR set | `docs/decisions/` | Owns why durable architectural decisions were made. |
| Current ownership map | Ownership registry | this file | Must change whenever a major owner or boundary changes. |

## Interaction owners

Endpoint owners do not jointly own an interaction. Each boundary has one owner responsible for the protocol/translation, sequencing, error semantics, lifecycle and integration proof appropriate to that boundary.

| Interaction | Single interaction owner | Status | Responsibility |
| --- | --- | --- | --- |
| Public caller -> assembled Images runtime | `createRuntime()` / public runtime surface | current: `src/runtime.js` | Owns package composition and public assembly without duplicating subsystem policy. |
| Graph/image semantics -> backend transaction seam | `GraphImageService` | current | Owns semantic record/history sequencing over backend transactions. |
| Backend contract -> Lagrange application session | Lagrange backend adapter | current | Sole translation between image backend operations and public Lagrange storage/session APIs. |
| Invocation request -> language dispatch resolution | `InvocationService` | current | Owns dispatcher invocation and construction of the activation envelope. |
| Activation -> executable representation | `ActivationExecutor` | current | Reads the code representation and delegates through the executor registry; executors do not self-select. |
| Activation -> transient authority checks | `ActivationExecutor` | current | Owns authority lifetime/propagation/attenuation plumbing; `AuthorityService` remains owner of the grant algebra. |
| WASM host effect -> ordinary message/closure execution | `ActivationExecutor` | current | Host effects re-enter the ordinary execution/send/closure path; WASM does not create a second language runtime. |
| Symmetric Smalltalk language policy -> generic execution | `createRuntime()` composition root | current | Registers language-owned primitives/temporary initialization without making `src/execution` depend on `src/language`. |
| CompilationService -> compiler registries | `CompilationService` | current | Owns compiler selection request, compilation-group orchestration and publication of derived artifacts. |
| ToolchainService -> artifact graph/provider | `ToolchainService` | current | Owns explicit input closure, provider boundary, diagnostics/result installation and provenance/reuse. |
| Durable runtime definition -> live foreign runtime | `ForeignRuntimeDefinitionService` | current | Resolves durable definitions/provider bindings and starts/reuses transient runtimes through `ForeignRuntimeService`. |
| Foreign runtime provider -> external VM/process | Concrete foreign-runtime provider | current per provider | Provider owns its transport/process protocol; generic service owns lifecycle semantics above it. |
| Callable binding -> concrete implementation lane | Representation-specific callable executor | current: `src/callable/` + execution registry | Owns decoding one binding and invoking its declared implementation lane. |
| Component binding -> host imports/authority check | Component binding executor | current | Wires only declared imports and performs use-time checks through the activation's `require`. |
| Authorized image projection/mutation/creation/read -> GraphImageService | Image projection/mutation/creation/object-read binding executor | current: `src/callable/` | Owns boundary typing/version-token/error translation and the create-lane authority/id-minting; GraphImageService owns the image write/read itself. |
| Authorized image observation -> private history stream | Image observation binding executor | current: `src/callable/` | Owns the metadata-only invalidation feed: per-event `object/read` filtering inside the substrate, the opaque (AES-256-GCM) cursor contract, and the no-payload/no-raw-revision guarantee; GraphImageService owns the private history stream itself. |
| Cargo toolchain provider -> OCI process | Cargo/rustc OCI provider | current: `src/toolchain/` | Owns Cargo-specific materialization, closed-input OCI invocation and output extraction. |
| OpenSmalltalk/Cuis definitions/toolchain -> VM bridge | OpenSmalltalk/Cuis provider(s) | current: `src/foreign-runtime/`, `src/toolchain/` | Own provider-specific Cuis transport/toolchain protocol; generic lifecycle/toolchain services remain authoritative above it. |
| Cuis semantic export (extraction) -> `smalltalk/cuis-semantic-export-v1` artifact | OpenSmalltalk/Cuis toolchain provider | current: `src/toolchain/opensmalltalk-cuis-toolchain-provider.js` | Owns the fixed, provider-owned export `.st` script (run toolchain-stage before `saveAndQuitAs:`), the canonical manifest schema/sorting/normalization, and the semantic identity model (ADR 0072). Crosses the heap boundary as TEXT only — semantic Package/Class/Method identities, never a Spur oop. |
| Cuis semantic export manifest -> ordinary image objects | Image creation-batch binding executor (ADR 0067) | current: `src/callable/` (lane) + `src/language/cuis-export-materialization.js` (Cuis-adapter pure translator + representation-class schema) | Owns materializing the manifest into ordinary objects via the authorized atomic creation batch; the toolchain does not create image objects. The Cuis adapter (`src/language/cuis-export-materialization.js`) owns only the pure manifest->member-list translation and the `CuisExportPackage`/`CuisExportClass`/`CuisExportMethod` representation-class schema (language-owned, behaviorally boring — an instance REPRESENTS a Cuis entity, never the executable Lagrange Class). Semantic identity stays string data; ObjectRef is server-minted. `Cuis-Base` refs stay reserved identity strings, never materialized. |
 | Project working descriptor -> release/provenance/installation/reconciliation descriptors | Project model (`src/project/model.js`) | current (ADR 0073) | Owns pure validation/canonicalization and translation across the Project deployment boundary. Representation-specific content materialization (ADR 0075 decided owner: `src/project/graph-release-materialization.js`), authorization, durable install/recovery, migration/drift handling and Git effects remain separate future owners. |
| durable working Project + stable Image frontiers -> ProjectReleaseManifest + ProjectReleaseProvenance | Project release-capture coordinator (`src/project/release-capture.js`) | current (ADR 0073) | Owns ONLY the sequencing/stability of a truthful current capture: bracket each participating Image with `GraphImageService.frontier()` before/after, read the descriptor via the working-state owner, select via the model, read each selected member's current direct source record, hand an isolated snapshot to the caller's `materializeRecord`, and refuse the whole capture on any frontier movement. The model owns all release/provenance/releaseId semantics; the materializer owns representation-specific `{representation, contentIdentity}`; no graph hashing/serialization lives here. Persisting releases, install/deploy and historical reads remain separate future owners. ADR 0075: the coordinator owns the stable-current read session (`createStableCurrentReadSession`, implemented in this module): lazy per-Image bracket-on-first-read (frontier BEFORE the first record read), `getObject` derived through `getRecord` (the Project descriptor and its backing members share the host bracket), `assertStable` over every Image actually read (canonical-order conflict choice), `frontierMap` gated on proven stability. The direct-record capture path routes through it; the ADR 0075 transitive materialization path must consume the SAME session owner — two bracket implementations are a stop condition. |
| selected Project members -> representation-specific portable release material | Graph release materializer (`src/project/graph-release-materialization.js`) | current (ADR 0075) | Orchestrates the existing owners, absorbing none: receives selected members + a scoped `{getRecord}` read facade from the capture coordinator (never `GraphImageService`/backend/frontiers — narrowness by construction), calls `exportGraphBundle` ONCE with roots = selected member keys and an internalize-all-unpinned `referencePolicy`, REFUSES the release when any pinned ref is reachable (`ProjectGraphReleaseMaterializationError`; externals must be empty — the executable proof that source identity cannot enter releaseId), gives every selected member the whole-bundle `contentIdentity` (coarse-but-truthful manifest v1 identity), and owns the immutable `lagrange-project-release-material/v1` package (intrinsic + release-linkage validation incl. root-keys === member-keys). Never walks durable edges itself, never brackets frontiers, never defines manifest semantics, never publishes records. |
| GraphImageService durable records -> portable graph bundle (export) | Graph bundle model (`src/graph/bundle.js`) | current (ADR 0074) | Owns the closure walk + local-id assignment + internal/external classification + canonical assembly + content identity. Reads through `GraphImageService` (`getRecord`) and `referencesOfRecord`; never duplicates graph storage semantics or invents a second traversal. The Project coordinator is NOT the exporter; a Project graph materializer is a representation owner that calls this owner. |
| portable graph bundle -> target Image records (import) | Graph bundle model (`src/graph/bundle.js`) | current (ADR 0074, importer implemented; ADR 0076 preparation seam decided, pending) | One owner for both translation directions so identity/closure rules cannot diverge. `importGraphBundle` validates the whole bundle (`assertGraphBundleV1`: structure + closure completeness), verifies `expectedContentIdentity` before any minting/publication, resolves externals only through explicit caller bindings (pinned -> PinnedRef preserved faithfully, unpinned -> existing-target ObjectRef verified to exist), mints ALL target ids up front via the active crypto provider, rewrites refs generically (no per-kind importer), and commits the heterogeneous batch through ONE `ImageService.createRecords` call (insert-only, one backend transaction) — never the backend, never history, never the per-kind puts. Publication failures propagate with createRecords' all-or-none atomicity; no importer rollback/retry. ADR 0076 adds `prepareGraphBundleImport` (pending): the same owner returns a frozen effect-free `{roots, recordInputs, contentIdentity}` plan that `importGraphBundle` itself consumes; the localId->target map is never returned; no Project awareness, no callbacks, no extra-records hook. |
| ProjectInstallation/v1 <-> ordinary target-Image installation objects | Project installation-state translator (`src/project/installation-state.js`) | decided, pending implementation (ADR 0076) | Owns the durable storage translation ONLY: well-known head/snapshot/member Shapes + race-tolerant idempotent `ensureInstallationShapes` (schema is never installation state), the deterministic head id `lagrange-project-installation/<projectId>/head`, `materializeInstallationRecords` from an ALREADY canonical ProjectInstallation, and `readManagedProjectInstallation` assembly (head -> snapshot -> members -> `normalizeProjectInstallation`) with the ADR 0076 Decision 10 corruption taxonomy (surface, never repair/rescan/delete). Does NOT decide releaseId, plan upgrade, import material, or own transaction/recovery sequencing. |
| prepared release installation -> atomic target graph + durable installation state | Managed installation coordinator (`src/project/managed-installation.js`) | decided, pending implementation (ADR 0076) | Owns ONLY the managed lifecycle sequencing: `installManagedProjectRelease` three-outcome contract (absent -> atomic first install: linked preflight -> `prepareGraphBundleImport` -> `createProjectInstallation` -> installation record specs -> ONE `createRecords` batch; same releaseId -> return the recovered existing descriptor with NO new graph; different releaseId -> refuse naming current/desired releaseIds) and the loser-re-read conflict path on head `VersionConflict`. No graph semantics, no descriptor semantics, no storage translation, no upgrade execution, no authority. |
| Project release + material -> target graph + ProjectInstallation | Project release-installation coordinator (`src/project/release-installation.js`) | current (ADR 0075 Decision 8, implemented) | Owns ONLY the sequencing/preflight/handoff of ONE install: `installProjectRelease({images, targetImageId, release, material, crypto}) -> ProjectInstallation/v1` — normalize the release once (isolated snapshot for both preflight and descriptor), run the linked release<->material validation through the materializer's `validateProjectReleaseMaterialForRelease` owner (never re-implemented here), call `importGraphBundle` EXACTLY ONCE with `expectedContentIdentity` (the whole release graph in one atomic publication — cross-member sharing/cycles preserved), and hand the imported roots to the model's `createProjectInstallation`. No graph semantics, no installation/descriptor semantics, no durable installation state, no reconciliation/dedup, no authority, no cleanup/compensation arm (preflight makes post-import semantic failure impossible — ADR 0075 Decision 8). The documented crash window (import committed, descriptor not yet persisted by the caller) is recorded pressure for the durable/idempotent installation-storage slice, never worked around locally. |
| Lagrange Object Environment -> Lagrange Images | `ImageClientAdapter` in `lagrange-object-environment` | external/planned | One cross-repository interaction owner; this repository supplies public image semantics and must not create a competing UI adapter. |
| Beads task/memory -> agent work session | `bd prime` workflow | current/tool-owned | Operational context injection/work discovery; project-specific governance remains in `AGENTS.md`. |

## Ownership change protocol

Before implementing a new major subsystem or interaction:

1. identify the responsibility in the plan/Bead
2. name exactly one owner here
3. identify which existing owner loses or delegates responsibility, if any
4. state the boundary contract/invariants
5. identify the primary proof/tests
6. only then implement

A proposal that says responsibility is `shared`, `co-owned`, `handled on both sides`, or leaves the interaction owner implicit is incomplete.

## Detecting ownership drift

Treat these as defects:

- two services persist competing versions of the same semantic state
- two endpoints independently retry/reconcile the same interaction
- language policy is duplicated in generic execution or storage
- backend adapters start deciding image semantics
- lane-specific executors invent a second invocation/closure/authority model
- two registries select implementations for the same extension point
- a toolchain/provider bypasses the explicit artifact graph and reads ambient image state
- an integration test cannot say which component is responsible for a boundary failure

When drift is found, do not synchronize duplicate implementations. Restore one owner and make the other side consume it.
