# Roadmap (index)

The full roadmap lives at [docs/roadmap.md](docs/roadmap.md). This root file
exists because the repository's committed solve-quest planning layout expects
a top-level roadmap document; the durable ordering authority remains the
referenced file and the quest discipline under `solve/`.

## Milestones

- [ ] image-service HTTP surface under contract test
