// A compiled WASM Component binary. This is an implementation representation only: it
// says nothing about which callable interfaces it satisfies. One Component may back
// several interfaces, each through its own binding artifact.
const WASM_COMPONENT_V1 = 'wasm-component/v1';

export {WASM_COMPONENT_V1};
